- if (pacientes.publicado)
  div(class="bg-white form-container shadow rounded-lg")
     div(class="p-5 space-y-3")
      h3(class="text-2xl font-bold")=pacientes.nombre
      p(class="text-sm text-gray-600") Campaña en la que se conoció al paciente: 
        span(class="text-gray-800 font-bold")=pacientes.campaign.nombre
      a(href=`/pacientes/${pacientes.id}` class="bg-verde-pantone hover:bg-azul-tur w-full text-center block font-bold text-white p-2 uppercase rounded") Ver paciente
