extends ../layout/index
block styles
    style.
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, #46b8a9, #FFFFFF);
            background-size: cover;
            z-index: -1; 
        }
        .form-container {
            background-color: rgba(255, 255, 255, 0.5); 
            border-radius: 0.8rem; 
        }
        
block contenido
    div.py-10 
        h1.text-4xl.my-10.font-extrabold.text-center  Fundación México Sin Sordera A.C 
            span.font-normal 
        h2.text-center.text-2xl.font-extrabold=pagina

        div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")
            p(class=error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.fond-bold.text-center.text-white.uppercase= mensaje


            if !error
                a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Ya puedes iniciar sesion
